# Архитектура API Agent - Сборщика проектов

## 📋 Обзор

API Agent - это Express сервер на TypeScript, который принимает запрос на сборку проекта и возвращает готовый ZIP архив.

## 🔄 Поток работы

### 1. Запрос от клиента

```
POST /build
Content-Type: application/json

{
  "appName": "my-app",
  "packageManager": "npm",
  "framework": "react",
  "routing": "react-router",
  "styling": "tailwind",
  "stateManager": "redux-toolkit"
}
```

### 2. Процесс сборки (внутри `ProjectBuilder.build()`)

```
┌─────────────────────────────────────────────────┐
│ 1. Создание временной директории                │
│    /tmp/project-builds/my-app/                  │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 2. Копирование шаблона                          │
│    - Из templates/react/ts/                     │
│    - Или из node_modules/create-vite/           │
│    - Все файлы шаблона копируются               │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 3. Патчинг package.json                         │
│    - Читаем существующий package.json           │
│    - Добавляем зависимости от плагинов          │
│    - Мерджим dependencies и devDependencies     │
│    - Сохраняем обратно                          │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 4. Применение модификаций файлов                │
│    - От каждого плагина получаем modifications  │
│    - Группируем по файлам                       │
│    - Применяем: replace, append, prepend        │
│    - Пример: добавить import в main.tsx         │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 5. Создание дополнительных файлов               │
│    - От каждого плагина получаем файлы          │
│    - Создаем: routes, stores, configs и т.д.    │
│    - Пример: src/routes/index.tsx               │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 6. Установка зависимостей                       │
│    npm install (или pnpm/yarn/bun)              │
│    - Устанавливаются все зависимости            │
│    - Создается node_modules/                    │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 7. Создание ZIP архива                          │
│    - Создается архив в /tmp/project-archives/   │
│    - Исключаются: node_modules, .git, .next     │
│    - Название: my-app-1234567890.zip            │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 8. Отправка архива клиенту                      │
│    - Content-Type: application/zip              │
│    - Content-Disposition: attachment            │
│    - Stream файла через pipe                    │
└─────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────┐
│ 9. Очистка временных файлов                     │
│    - Удаляется /tmp/project-builds/my-app/      │
│    - Удаляется архив (через 5 сек)              │
└─────────────────────────────────────────────────┘
```

## 📁 Структура директорий

### Временные директории (создаются автоматически)

```
/tmp/
├── project-builds/          # Временные проекты
│   └── my-app/             # Собранный проект
│       ├── src/
│       ├── package.json
│       ├── node_modules/   # После npm install
│       └── ...
└── project-archives/        # ZIP архивы
    └── my-app-1234567890.zip
```

### Структура кода

```
api-agent/
├── src/
│   ├── index.ts              # Express сервер, роуты
│   ├── builder.ts            # ProjectBuilder класс
│   ├── types.ts              # TypeScript типы
│   ├── helpers/              # Вспомогательные функции
│   │   ├── copy.ts           # Копирование файлов
│   │   └── install.ts        # Установка зависимостей
│   └── plugins/              # Плагины
│       ├── index.ts          # Загрузчик плагинов
│       ├── react-router.ts
│       ├── redux-toolkit.ts
│       ├── tailwind.ts
│       └── ...
└── templates/                # Локальные шаблоны (опционально)
    ├── react/
    │   ├── ts/
    │   └── js/
    └── ...
```

## 🔧 Ключевые компоненты

### 1. Express Server (`src/index.ts`)

**Основные функции:**
- Принимает POST запросы на `/build`
- Валидирует входные данные
- Создает `ProjectBuilder`
- Отправляет ZIP архив клиенту
- Управляет очисткой временных файлов

**Обработка ошибок:**
- Если ошибка на любом этапе - очистка временных файлов
- Если архив не найден - возврат ошибки
- Если ошибка отправки - очистка ресурсов

### 2. ProjectBuilder (`src/builder.ts`)

**Основные методы:**

```typescript
class ProjectBuilder {
  async build(): Promise<BuildResult>
    // Главный метод - выполняет всю сборку
  
  private async copyTemplate()
    // Копирует шаблон из templates/ или create-vite
  
  private async patchPackageJson()
    // Патчит package.json, добавляя зависимости от плагинов
  
  private async applyModifications()
    // Применяет модификации файлов от плагинов
  
  private async createAdditionalFiles()
    // Создает дополнительные файлы от плагинов
  
  private async createArchive(): Promise<string>
    // Создает ZIP архив проекта
  
  async cleanup(): Promise<void>
    // Удаляет временную директорию проекта
  
  async cleanupArchive(archivePath: string): Promise<void>
    // Удаляет архив
}
```

### 3. Плагины (`src/plugins/`)

**Структура плагина:**

```typescript
interface Plugin {
  getDependencies(config): PluginDependencies
    // Возвращает зависимости для package.json
  
  getModifications(config): PluginModification[]
    // Возвращает модификации файлов
  
  getAdditionalFiles(config): FileDefinition[]
    // Возвращает дополнительные файлы для создания
}
```

**Пример: Tailwind Plugin**

```typescript
{
  getDependencies: () => ({
    devDependencies: {
      tailwindcss: '^4.0.0',
      '@tailwindcss/postcss': '^4.0.0'
    }
  }),
  
  getModifications: () => [
    {
      file: 'src/index.css',
      type: 'append',
      content: '@import "tailwindcss";\n'
    }
  ],
  
  getAdditionalFiles: () => [
    {
      path: 'tailwind.config.ts',
      content: '...'
    }
  ]
}
```

## 🔑 Важные моменты

### Патчинг package.json

**Как это работает:**

1. Читаем базовый `package.json` из шаблона
2. Для каждого выбранного плагина:
   - Получаем `dependencies` и `devDependencies`
   - Мерджим их в существующий `package.json`
3. Резолвим конфликты версий (если нужно)
4. Удаляем пустые секции (`devDependencies` если пусто)
5. Сохраняем обратно

```typescript
// До
{
  "dependencies": { "react": "^18.2.0" }
}

// После добавления Redux и Tailwind
{
  "dependencies": {
    "react": "^18.2.0",
    "@reduxjs/toolkit": "^2.0.0",
    "react-redux": "^9.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0"
  }
}
```

### Модификация файлов

**Типы модификаций:**

1. **replace** - замена паттерна
   ```typescript
   {
     file: 'src/main.tsx',
     type: 'replace',
     pattern: '<App />',
     replacement: '<Provider><App /></Provider>'
   }
   ```

2. **append** - добавление в конец
   ```typescript
   {
     file: 'src/index.css',
     type: 'append',
     content: '@import "tailwindcss";\n'
   }
   ```

3. **prepend** - добавление в начало
   ```typescript
   {
     file: 'src/main.tsx',
     type: 'prepend',
     content: 'import { Provider } from "react-redux";\n'
   }
   ```

### Очистка временных файлов

**Когда происходит:**

1. **После успешной отправки архива:**
   - Удаляется директория проекта (`/tmp/project-builds/my-app/`)
   - Архив удаляется через 5 секунд (задержка на случай если клиент еще качает)

2. **При ошибке:**
   - Немедленная очистка всех временных файлов
   - Удаление архива если был создан

3. **При ошибке отправки:**
   - Очистка в обработчике ошибок stream

## 📊 Пример полного запроса

### Request

```bash
POST http://localhost:3000/build
Content-Type: application/json

{
  "appName": "my-awesome-app",
  "packageManager": "npm",
  "framework": "react",
  "routing": "react-router",
  "styling": "tailwind",
  "stateManager": "redux-toolkit"
}
```

### Response

**Headers:**
```
Content-Type: application/zip
Content-Disposition: attachment; filename="my-awesome-app-1234567890.zip"
```

**Body:**
- ZIP файл с проектом

### Что происходит внутри:

1. ✅ Валидация запроса
2. ✅ Создание `/tmp/project-builds/my-awesome-app/`
3. ✅ Копирование шаблона React + Vite + TypeScript
4. ✅ Патчинг package.json:
   - Добавляется `react-router-dom`
   - Добавляется `@reduxjs/toolkit`, `react-redux`
   - Добавляется `tailwindcss`, `@tailwindcss/postcss`
5. ✅ Модификация файлов:
   - Добавление импортов в `src/main.tsx`
   - Обертывание App в Provider и Router
   - Добавление `@import "tailwindcss"` в CSS
6. ✅ Создание файлов:
   - `src/routes/index.tsx`
   - `src/store/index.ts`
   - `tailwind.config.ts`
7. ✅ `npm install` - установка всех зависимостей
8. ✅ Создание ZIP архива (без node_modules)
9. ✅ Отправка архива клиенту
10. ✅ Очистка временных файлов

## 🛠️ Настройка

### Изменение временных директорий

В `builder.ts`:
```typescript
constructor(private config: MasterConfig) {
  // Можно изменить пути
  this.workDir = path.join(os.tmpdir(), 'project-builds');
  this.archiveDir = path.join(os.tmpdir(), 'project-archives');
}
```

### Изменение задержки удаления архива

В `index.ts`:
```typescript
setTimeout(async () => {
  // Изменить задержку (в миллисекундах)
}, 5000); // 5 секунд
```

### Добавление новых плагинов

1. Создать файл в `src/plugins/your-plugin.ts`
2. Реализовать интерфейс `Plugin`
3. Добавить в `src/plugins/index.ts`:
```typescript
if (config.yourFeature === 'your-plugin') {
  plugins.push(yourPlugin);
}
```

## 🔍 Отладка

### Логи

Агент выводит подробные логи на каждом этапе:
```
🚀 Starting build for project: my-app
📦 Copying template...
📝 Patching package.json...
🔧 Applying plugin modifications...
📄 Creating additional files...
📥 Installing dependencies...
📦 Creating archive...
✅ Archive created: /tmp/project-archives/my-app-1234567890.zip
✅ Build completed successfully
🧹 Cleaning up temporary files...
```

### Проверка временных файлов

Если нужно проверить собранный проект:
- Отключите очистку временно (закомментируйте `cleanup()`)
- Проверьте `/tmp/project-builds/my-app/`

## ⚠️ Важные замечания

1. **node_modules не включается в архив** - это правильно, клиент установит сам
2. **Архив удаляется через 5 секунд** - если клиент медленно качает, увеличьте задержку
3. **Очистка происходит всегда** - даже при ошибках, чтобы не засорять диск
4. **Шаблоны могут быть локальными или из create-vite** - настраивается в `getTemplatePath()`


